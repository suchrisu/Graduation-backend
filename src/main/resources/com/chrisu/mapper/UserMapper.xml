<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.chrisu.mapper.UserMapper">

  <resultMap id="userResultMap" type="com.chrisu.POJO.User">
    <result column="user_id" property="userId"/>
    <result column="role_power_id" property="rolePowerId"/>
    <result column="user_name" property="userName"/>
    <result column="user_password" property="userPassword"/>
    <result column="user_register_time" property="userRegisterTime"/>
    <result column="user_header" property="userHeader"/>
  </resultMap>
  <update id="update">
    update user
    <set>
      <if test="rolePowerId != null and rolePowerId != ''">
        role_power_id =
        #{rolePowerId},
      </if>
      <if test="userName != null and userName != ''">
        user_name =
        #{userName},
      </if>
      <if test="userPassword != null and userPassword != ''">
        user_password =
        #{userPassword},
      </if>
      <if test="userHeader != null and userHeader != ''">
        user_header =
        #{userHeader},
      </if>
    </set>
    where user_id = #{userId}
  </update>
  <delete id="deleteByIds">
    delete from user where user_id in
    <foreach collection="ids" item="id" separator="," open="(" close=")">
      #{id}
    </foreach>
  </delete>


</mapper>